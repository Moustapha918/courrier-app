/*
 serveimport { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ArrivedMailScComponent } from './arrived-mail-sc.component';

describe('ArrivedMailScComponent', () => {
  let component: ArrivedMailScComponent;
  let fixture: ComponentFixture<ArrivedMailScComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ArrivedMailScComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ArrivedMailScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
